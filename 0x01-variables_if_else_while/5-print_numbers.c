#include <stdio.h>
/**
 *main-Code entry point
 *Return:0 if there are no errors,non zero if there re errors
 **/
int main(void)
{
	char i;

	for (i = '0'; i <= '9'; i++)
	putchar(i);
	putchar('\n');
	return (0);
}
